# Copy this file to esp.env and adjust for your workstation.
# Keep values unquoted unless the path contains whitespace.
# Before running idf.py, bootstrap_env.sh, or monitor.py, run:
#   source ./tools/setup.sh

IDF_PATH=/path/to/esp-idf
BOARD_PORT=/dev/ttyACM0
